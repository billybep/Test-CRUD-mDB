# Test-CRUD-mDB

**ROUTES**

- GET /users
- GET /users/:identityNumber

- POST /users
- PUT /users/:id
- DELETE /users/:id

**REQUIRE**

- Header </br>
  **access_token** </br>
  `access_token: 'any_string'`